services:
  tunnel:
    image: cloudflare/cloudflared:latest
    container_name: snack-tunnel
    restart: always
    environment:
      - TUNNEL_TOKEN=${TUNNEL_TOKEN}
    command: tunnel --no-autoupdate run
    depends_on:
      - frontend
    networks:
      - snack-net
