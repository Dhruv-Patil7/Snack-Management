ALTER TABLE redemptions ADD COLUMN snack_item VARCHAR(100);

CREATE TABLE daily_menu (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    menu_date DATE NOT NULL UNIQUE,
    snack_name VARCHAR(100) NOT NULL
);
